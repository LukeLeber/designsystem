{% set attributes = create_attribute(attributes|default({})) %}

{% set classes = [
  'llds-layout'
] %}
<div{{ attributes.addClass(classes) }}>
  <div class="llds-layout__content">
    {{ content }}
  </div>
</div>
