{% set attributes = create_attribute(attributes|default({})) %}

{% set classes = [
  'llds-block',
  alignment in ['left', 'center', 'right'] ? 'llds-block--' ~ alignment - 'aligned',
] %}

<div{{ attributes.addClass(classes) }}>
  {{ content }}
</div>
