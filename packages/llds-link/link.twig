{% set attributes = create_attribute(attributes|default({})) %}

{% set classes = [
  'llds-link',
  type in ['primary-cta', 'secondary-cta'] ? 'llds-link--' ~ type
] %}

<a{{ attributes.addClass(classes) }}>{{ content }}</a>