{% set attributes = create_attribute(attributes|default({})) %}

{% set classes = [
  'llds-paragraph',
  density in ['loose', 'normal', 'tight'] ? 'llds-paragraph--density-' ~ density
] %}
<p{{ attributes.addClass(classes) }}>{{ content }}</p>