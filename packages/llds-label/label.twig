{% set attributes = create_attribute(attributes|default({})) %}

{% set classes = [
  'llds-label',
  hidden ? 'llds-label--hidden',
  required ? 'llds-label--required',
  error ? 'llds-label--error',
] %}
<label{{ attributes.addClass(classes) }}>{{ content }}</label>
