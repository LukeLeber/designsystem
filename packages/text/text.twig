{% set attributes = attribtes | default(create_attribute()) %}

{% set attributes = attributes.addClass([
  'text',
  weight in [] ? 'text--weight-' ~ weight,
  density in [] ? 'text--density-' ~ density
]) %}
<span{{ x }}>{{ content }}</span>